# OmniForge 路线图

> 版本：v1.0（2026-09-11）｜配套：[PROJECT_CHARTER.md](PROJECT_CHARTER.md)｜待办明细：[docs/product/backlog.md](docs/product/backlog.md)

排期依据：仓库 2026-07-04 启动、累计 50 次提交，最近一次提交为 **2026-08-13（已静默约 4 周）**。
因此第一阶段的目标之一是**恢复交付节奏**：每个里程碑都要有可演示、可验收的产出。

---

## 里程碑总览

| 里程碑 | 主题 | 出口（退出标准） | 预估 |
|---|---|---|---|
| **M0** | 安全与可信 | 高危安全项清零；CI 在无密钥环境可跑通；测试不污染真实数据 | 1–2 周 |
| **M1** | 可用与一致 | 空壳功能处置完毕；能从零部署并完成数据恢复演练；README 与实现零漂移 | 1–2 周 |
| **M2** | 稳定与可观测 | 有 SLO 与基线报表；监控告警可用；成本可观测且可限额 | 1–2 周 |
| **M3** | 体验与增长 | 前端缺口补齐；评测集扩充；发布流程（版本/变更日志）跑顺 | 持续 |

---

## M0 · 安全与可信（最高优先级）

**为什么先做**：技术评审已确认 4 个高危漏洞（含可复现的 Python 沙箱逃逸），
且 CI 在无密钥环境下必红、测试会写入真实数据库与反馈文件——**质量门禁当前不可信**。

范围：

- 前端 Markdown 渲染的 XSS 防护（关闭原始 HTML 或接入净化库）
- Python 沙箱逃逸加固（阻断 `io`/`_io`/`codecs` 与 `open`，清理危险模块）
- DocxTool 路径穿越修复（复用文件系统工具的工作区包含校验）
- `/workspace/set` 任意路径读写收敛（会话化 + 基目录限制）
- CI：无密钥可跑、凭据缺失时跳过相关用例；Windows 临时目录清理
- 测试隔离：引入 `tests/conftest.py`，禁止写真实 `agentflow.db` 与 `data/feedback`

退出标准：`docs/product/backlog.md` 中 P0-SEC-1..4、P0-CI-1..3 全部关闭并复测通过。

## M1 · 可用与一致

范围：

- 空壳工具（Browser / MCP / Database）二选一：实现最小可用版本，或从 README/UI 下线
- 死代码处置（ProjectStructurePlanner）与前端未接线功能（工作区浏览、知识库重建 UI）
- 异步阻塞修复（LLM/SQLite/文件解析不阻塞事件循环；SSE 资源释放）
- 数据安全：备份脚本 + 一次真实恢复演练；K8s 缺失 `EMBEDDING_API_KEY` 的启动校验
- 文档一致性：README、截图、任务数/目标类型声明与实现对齐

退出标准：新环境按 `docs/deployment.md` 从零部署成功；按 `docs/runbook.md` 完成一次恢复演练；
端到端冒烟脚本通过。

## M2 · 稳定与可观测

范围：

- 定义 SLO（可用性、P95 延迟、任务成功率、平均轮次）并选定采集方式（现有 `/metrics`）
- 告警规则：健康检查失败、LLM 熔断打开、降级比例异常、磁盘水位
- 成本看板：按节点/会话统计 token 与费用，支持日预算上限
- 失败样本例会：`data/feedback/*.jsonl` → 每周转为评测集或 Issue

退出标准：连续两周有 SLO 报表与告警演练记录；成本报表可按日查看。

## M3 · 体验与增长

范围：

- 评测集扩充与基线对比（意图/规划/工具/完成度/RAG 五套）
- 前端体验（会话重命名、任务树完善、错误提示不静默吞掉）
- 发布流程：语义化版本、`CHANGELOG.md`、镜像 tag、回滚演练
- 可选增强：检索重排默认开启评估、工具级超时与瞬时重试

---

## 节奏与工作方式

| 事项 | 约定 |
|---|---|
| 迭代长度 | 1 周；每周至少 1 个可验收产出 |
| 演示 | 每两周一次固定脚本演示（对话 → 生成项目 → 知识库检索） |
| 评审 | 每个里程碑结束做一次自评，更新 `docs/risk-register.md` |
| 变更控制 | 里程碑范围变更需在 `docs/product/backlog.md` 记录原因 |
| 质量门禁 | DoD：测试全绿 + 覆盖率不低于基线 + lint 干净 + 文档同步（见 [CONTRIBUTING.md](CONTRIBUTING.md)） |

## 当前阻塞与依赖

1. **定位未定**（章程 D1）——影响 M0 之后是否继续投入安全项。
2. **单人产能**——M0/M1 需要连续投入，建议先锁定两周。
3. **外部 API 额度**——CI 与评测需要可用的模型/embedding 凭据（或 Mock 方案）。
