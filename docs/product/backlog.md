# 待办清单（Backlog）

> 来源：技术评审报告（[docs/reviews](../reviews/2026-08-14-full-review.md)）+ 工程现状核对。
> 建议逐条转为 GitHub Issue 跟踪；验收标准为该条目的关闭条件。

优先级：**P0** 阻断（安全/可信） → **P1** 正确性与一致性 → **P2** 优化与增强。

---

## P0 · 安全（对外开放前必须清零）

| ID | 标题 | 证据 | 验收标准 |
|---|---|---|---|
| P0-SEC-1 | 前端 Markdown 渲染存在存储型 XSS | 评审 H4：`MarkdownRenderer` 开启原始 HTML | 关闭 `html:true` 或接入净化库；构造恶意 Markdown 用例验证不执行脚本 |
| P0-SEC-2 | Python 沙箱可通过 `io`/`_io`/`codecs` 等逃逸 | 评审 H2（评审方已复现） | 阻断上述模块与 `open` 相关属性；wrapper 清理危险 `sys.modules`；补充逃逸测试用例回归 |
| P0-SEC-3 | DocxTool 存在路径穿越 | 评审 H3 | 复用文件系统工具的工作区包含校验；补路径穿越单测 |
| P0-SEC-4 | `/workspace/set` 可任意路径读写 | 评审 H1 | 会话化 + 基目录白名单限制；补越权访问测试 |

## P0 · 质量门禁可信度

| ID | 标题 | 证据 | 验收标准 |
|---|---|---|---|
| P0-CI-1 | 测试污染真实数据库与反馈文件 | 评审 §4：测试写入 `agentflow.db` 与 `data/feedback/feedback.jsonl` | 新增 `tests/conftest.py`，所有测试使用临时路径；跑完整套件后真实文件无变更 |
| P0-CI-2 | CI 在无密钥环境必红 | 评审 §4 | 无 API Key 时相关用例跳过或使用 Mock；CI 全绿 |
| P0-CI-3 | Windows 上临时目录清理报错（83 例） | 评审 §4 | 关闭文件句柄/幂等 `close()`；Windows CI 无清理错误 |
| P0-CI-4 | 环境相关用例不稳定（health、workflow 冒烟） | 评审 §4.1–4.4 | 用例与环境解耦；连续 3 次运行结果一致 |

> **状态（2026-09-12）**
> - ✅ P0-CI-2 已关闭：补齐 `python-multipart` 依赖、测试去除对本机密钥的隐式依赖后，
>   CI 在 ubuntu / windows 双平台均为绿（GitHub Actions 运行成功，539 passed）。
> - ✅ P0-CI-3 已关闭：CI 已无临时目录清理报错。
> - ✅ P0-CI-4 已关闭：`/health` 与 workflow 冒烟用例改为断言接口契约，
>   分别覆盖"密钥已配置 / 未配置"两种状态。
> - ❌ P0-CI-1 未关闭：跑完 539 项测试后 `data/feedback/feedback.jsonl` 无变更，
>   但 `agentflow/database/agentflow.db` 仍被改写，需要 `tests/conftest.py` 级的临时路径隔离。

## P1 · 正确性

| ID | 标题 | 证据 | 验收标准 |
|---|---|---|---|
| P1-ASYNC-1 | 同步调用阻塞事件循环（LLM/SQLite/解析），退避用 `time.sleep`，SSE 流未释放 | 评审 M1/M2 | 流式走异步客户端；阻塞调用移出事件循环；SSE 结束显式释放；并发压测下不出现明显阻塞 |
| P1-ASYNC-2 | SQLite 连接泄漏（初始化未关闭、`close()` 非幂等） | 评审 M11（已复现） | 修复并补连接生命周期单测；重复 `close()` 不报错 |
| P1-PLAN-1 | 非项目路径用新计划**整体替换**任务队列，replan 时丢弃在途任务 | 评审附录 A | 非项目路径也走合并逻辑；补 replan 保留在途任务的单测 |
| P1-PY-1 | 无代码块时任务被标记失败（正常场景进 replan）；忽略任务 input 中的 `code` | 评审附录 A | 无代码视为跳过而非失败；优先执行任务指定代码 |
| P1-MEM-1 | 滚动摘要不跨轮持久化；20 条上限使压缩几乎失效 | 评审 §1 | 摘要写入会话状态并跨轮复用；补长会话记忆测试 |
| P1-ANS-1 | 降级错误分类恒为 unknown；`metadata.pop` 就地修改会话状态 | 评审 §1/附录 A | 错误分类准确；不再就地修改输入状态 |
| P1-REF-1 | 反思器任务匹配为子串匹配，多结果塌缩；工作区扫描异常冒泡 | 评审附录 A | 改为精确/映射匹配；扫描异常走规则兜底 |
| P1-QUEUE-1 | 任务状态更新接受任意键，未知状态抛错 | 评审附录 A | 状态枚举校验 + 未知值降级处理 |
| P1-API-1 | 路由层缺输入校验、负 limit 未钳制、异常出口不统一 | 评审 M5/M10 | 补 Pydantic 校验与统一异常处理；补边界用例 |

## P1 · 一致性（文档 = 实现）

| ID | 标题 | 验收标准 |
|---|---|---|
| P1-DOC-1 | ~~空壳工具（Browser/MCP/Database）处置~~ → ✅ 已完成（2026-09 移除占位实现，README/status 同步） | 如需该能力，另立"实现浏览器/MCP/数据库工具"条目（见 P2-TOOL-2） |
| P1-DOC-2 | README 计数与格式声明修正 | 任务上限、目标类型数、支持格式声明与实现一致 |
| P1-DOC-3 | 截图替换为真实界面 | README 中引用真实截图 |
| P1-FE-1 | 前端缺口补齐或下线 | 会话重命名、工作区浏览、知识库重建入口：接线或从 UI 移除；补输入法组合态守卫与竞态修复 |
| P1-FE-2 | 前端错误不再静默 | 关键操作失败有可见提示 |

## P2 · 优化与增强

| ID | 标题 | 价值 |
|---|---|---|
| P2-DEAD-1 | 清理死代码 → ✅ 项目结构规划器已删除；剩余：composio `_ACTION_MAP`、answer 空桩 | 降低维护成本 |
| P2-TOOL-2 | 实现浏览器自动化 / MCP / 数据库工具（原占位实现已移除） | 扩展能力边界 |
| P2-TEST-1 | 覆盖率 64% → 75%（核心模块优先），补评测框架 runner/generate/tune 测试 | 质量门禁强化 |
| P2-OBS-1 | 接入 Prometheus/Grafana + 告警规则（健康、熔断、降级比例、磁盘） | 可观测性 |
| P2-COST-1 | 成本看板与日预算告警 | 成本可控 |
| P2-BACKUP-1 | 备份脚本自动化 + 恢复演练常态化 | 数据安全 |
| P2-EVAL-1 | 用运行时反馈扩充评测集 | 指标代表性 |
| P2-TOOL-1 | 工具级超时 + 瞬时错误自动重试 | 稳定性 |
| P2-PARSE-1 | `.doc` 解析修复或从支持列表移除 | 声明一致 |
| P2-ARCH-1 | 横向扩展方案预研（共享数据库 + RWX 存储） | 容量上限 |

---

## 建议的执行顺序（前两周）

1. P0-SEC-1..4（安全红线）
2. P0-CI-1..4（让门禁可信）
3. P1-ASYNC-1/2（稳定性与资源）
4. P1-DOC-1/2（一致性，成本最低、收益直接）
